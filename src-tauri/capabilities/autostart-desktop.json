{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "autostart-desktop",
  "description": "Allow toggling launch-on-login",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["autostart:default"]
}
